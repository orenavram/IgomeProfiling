The main script is called IgOmeProfiling_pipeline.py. In order to run it write in the command line the following and you will see the manual of the application:
python IgOmeProfiling_pipeline.py -h

You can find some test data at mock_data folder.